# 2026-05-27 Desktop review width

## Summary

- Increased desktop `body` max width from `760px` to `1120px`.
- Slightly increased page padding on desktop.
- Removed the review comparison table's desktop `720px` minimum width so it can fit the wider body.
- Kept the `720px` table minimum inside the mobile media query, preserving horizontal scroll on narrow screens.
- Adjusted the fixed record/play columns slightly for the wider table.

## Verification

- `git diff --check`
