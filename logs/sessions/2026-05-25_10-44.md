# Session Log: 2026-05-25 10:44

## Goal

先將 Places GAS 與 Supabase 的連線方式改回 legacy `service_role` JWT，避開新版 `sb_secret_*` key 在 Google Apps Script `UrlFetchApp` 環境下被 Supabase 判定為 browser use 而回傳 401 的問題。

## Changes

- 修改 `places-gas/gas/程式碼.js` 的 `getSupabaseHeaders_()`。
- REST API header 現在一律包含：
  - `apikey: <SUPABASE_SERVICE_ROLE_KEY>`
  - `Authorization: Bearer <SUPABASE_SERVICE_ROLE_KEY>`
- 移除 `sb_secret_*` key 不帶 Bearer token 的特殊處理。
- 保留 `SUPABASE_URL` 與 `SUPABASE_SERVICE_ROLE_KEY` 從 Apps Script Properties 讀取，不回到硬寫 key。

## Deployment

- 在 `places-gas/` 執行 `clasp push`。
- 推送成功：`Pushed 6 files at 上午10:44:32.`

## Next Check

Apps Script Properties 中的 `SUPABASE_SERVICE_ROLE_KEY` 需要放 legacy `service_role` JWT，而不是新版 `sb_secret_*` secret key。完成後再從 Places 試跑 `syncFinalTasksToSupabase()`。
